# Telemetry Hub — ТЗ и статус проекта

## 1. Общее описание
Программный комплекс **Telemetry Hub** для круглосуточного (24/7) мониторинга и диспетчеризации контроллеров **КСИТАЛ GSM**, **RADS CCU-825** и **ОВЕН ПЛК**.
Система разработана для автономной работы и устойчива к внезапным отключениям электроэнергии (автоматическое восстановление, предотвращение коррупции файлов базы данных).

> **Статус релиза (версия 2.0.0):** Полнофункциональный релиз v2.0.0 на WinUI 3. Подключена фирменная иконка приложения и инсталлятора, реализован полнофункциональный **Журнал событий** (сохранение тревог, отчетов телеметрии, запросов/команд оператора и служебных SMS с ротацией ровно до 100 записей). Стартовая вкладка по умолчанию — Журнал событий. Заменена устаревшая терминология «Квитировать» на «Подтвердить» / «Подтверждено» / «Подтвердить все».

## 2. Архитектура проекта
* **Core**: Доменные модели (`MonitoredObject`, `AlarmEvent`), нормализация телефонов E.164 (`PhoneNumber`), генератор SMS-команд, реестр парсеров (`TelemetryParserRegistry`), а также `ImportExportService` для работы с Excel и CSV.
* **Modem.Engine**: Модуль связи с GSM-модемом по COM-порту (AT-команды, декодирование PDU UCS-2/7-bit).
* **Parser.***: Специфичные модули разбора сообщений для контроллеров КСИТАЛ, RADS CCU-825 и ОВЕН ПЛК.
* **Storage.Sqlite**: База данных SQLite. Включен режим **WAL** и `DefaultTimeout=5` для защиты данных и предотвращения блокировок при конкурентном доступе между UI и фоновой службой. Таблица `SystemStatus` для бесконфликтного IPC. Автоматическая ротация журнала событий до 100 записей (`RotateJournalEventsAsync`).
* **Service.Worker**: Системная служба Windows (Windows Service) для фонового сбора данных 24/7 без необходимости активного сеанса пользователя. Монопольно владеет COM-портом. Фиксирует отправку команд и служебных сообщений в Журнале событий.
* **UI.WinUI**: Диспетчерский интерфейс на **WinUI 3 / Windows App SDK** (Fluent Design, светлая/темная тема, сгруппированные карточки, Журнал событий, управление объектами, импорт/экспорт, автобэкап и проверка обновлений с GitHub).
* **Tools.Simulator**: Утилита генерации тестовых событий и тревог для отладки UI без модема.

## 3. Развертывание и обновления
* **Сборка**: Self-contained пакет под `win-x64` версии **2.0.0**. Установщик генерируется через **Inno Setup** (с фирменной иконкой, сохранением `telemetry.db` при накате обновлений, автоматической регистрацией службы и созданием ярлыков).
* **CI/CD**: Настроен GitHub Actions (`release.yml`). Сборка запускается автоматически при пуше тега версии. Готовый `telemetry-hub-setup-*.exe` публикуется в релизах GitHub.
* **Автоматизация**: Локальные скрипты `release_patch.bat` и `release_minor.bat` (вызывают `bump_release.ps1`), которые запрашивают Release Notes, автоматически повышают версию проекта в `.csproj` и пушат тег на сервер.
* **CLI Runner**: `run_winui.ps1` для быстрой сборки и запуска рабочего места в среде Windows PowerShell.

## 4. Текущий статус
* [x] Разработка базовой архитектуры, модулей декодирования PDU и парсеров контроллеров.
* [x] Настройка службы сбора данных `Service.Worker` (Windows Service).
* [x] Отказоустойчивость: детерминированная инициализация SQLite в WAL-режиме с `PRAGMA synchronous = NORMAL`.
* [x] Межпроцессное взаимодействие без аппаратных коллизий: таблица `SystemStatus` для передачи статуса модема и CSQ в UI.
* [x] Создание интерфейса на WinUI 3 (Windows App SDK) с навигацией и карточками объектов.
* [x] Управление объектами (добавление, редактирование, быстрые команды) через модальные окна.
* [x] Импорт и экспорт базы объектов (XLSX, CSV от GSM Guard) с автонормализацией типов и телефонов.
* [x] Автоматизация сборок: Inno Setup и CI/CD (GitHub Actions).
* [x] Очистка кодовой базы от костылей, ребрендинг в Telemetry Hub.
* [x] Полный отказ от legacy WPF интерфейса и переход исключительно на современный WinUI 3.
* [x] **Релиз v2.0.0**: подключение иконки приложения и установщика (`app.ico`).
* [x] **Журнал событий**: фиксация тревог, отчетов телеметрии, команд и служебных SMS с ротацией до 100 записей.
* [x] Стартовый экран по умолчанию переведен на Журнал событий.
* [x] Замена терминологии «Квитировать» на «Подтвердить» / «Подтверждено» / «Подтвердить все».
* [x] Очистка параметров: скрыт путь к рабочей БД (автоматически размещается в каталоге приложения).
* [x] **Группировка объектов по районам**: аккордеоны (Fluent Expanders) с бейджами количества и индикацией активных тревог.
* [x] **Всплывающее окно тревоги (AlarmWindow TopMost)**:
  - Автоматическое всплытие поверх всех окон с размером 620×640 px.
  - Собственный резкий 3-тональный звуковой сигнал тревоги (880 -> 1200 -> 1500 Гц, PCM WAV).
  - Кнопка «Подтвердить» (квитирование тревоги оператором с фиксацией в журнале).
  - Кнопка «Отложить (5 мин)» и автооткладывание при закрытии окна крестиком `[X]`.
  - Фоновый контроль повторного оповещения через 5 минут для неподтвержденных тревог.
* [x] **Автономный запуск и симуляция**:
  - `run_winui.bat` запускает предсобранные бинарники мгновенно без перекомпиляции.
  - `send_test_alarm.bat` для имитации прихода SMS извне с поиском активной БД приложения.
* [x] **Устранение технологических рисков релиза (Риски №1 и №4)**:
  - **Права доступа инсталлятора (Риск 1)**: директива `Permissions: users-modify` для `{app}` в `installer.iss`. Приложение и SQLite работают у обычного оператора без запросов UAC администратора независимо от диска установки (`C:\Program Files`, `C:\TelemetryHub`, `D:\...`).
  - **Синхронизация базы службы и UI**: детерминированное разрешение `telemetry.db` службой `Service.Worker` из подпапки `WorkerService`.
  - **Гарантированная очистка SIM-карты (Риск 4)**: удаление SMS (`AT+CMGD`) в блоке `finally` в `GsmModemClient`, защита от переполнения памяти модема.
  - **Фильтрация спама и служебных SMS**: сообщения от неизвестных номеров и оператора (баланс, реклама) пишутся в Журнал событий как `Service` с ротацией до 100 записей и НЕ создают фантомных объектов в диспетчере котельных.
  - Схема БД: `MonitoredObjectId` в `Alarms` сделан nullable (`int?`) с автомиграцией существующей базы SQLite.
* [x] **Импорт базы объектов из GSMGuard (`dist/BackupData20260918.txt`)**:
  - Реализован парсер `ImportFromGsmGuard` в `ImportExportService` с декодированием `Windows-1251` через `CodePagesEncodingProvider`.
  - Парсинг 251 блока секций `[Object_N]` с дедупликацией по номеру телефона (ровно **228 уникальных объектов**).
  - Корректный маппинг полей: `Name` $\rightarrow$ Название, `DevicePhone`/`Phone1` $\rightarrow$ Телефон, `Address` $\rightarrow$ Район/участок (Вешкайма, Сенгилей, Карсун и др.), `Password` $\rightarrow$ Пароль (с дефолтом `"00000"`), `DeviceModel` $\rightarrow$ `DeviceType` (Кситал — 200 с учетом АСТ/<Неизвестно>, CCU825 — 22, ОВЕН — 6).
  - Добавлена кнопка **«Импорт из GSMGuard (.txt)»** в Параметрах интерфейса WinUI с upsert в базу SQLite и мгновенным обновлением списка котельных и районов.
  - Разработаны автоматические тесты `GsmGuardImportTests.cs` (все 23/23 теста в репозитории проходят успешно).

## 5. Планы на следующий этап
* [ ] Тестирование работы интерфейса с реальной базой 228 объектов (время старта, плавность раскрытия районов, поиск).
* [ ] Выгрузка сводных аналитических отчетов телеметрии за период в Excel/CSV.